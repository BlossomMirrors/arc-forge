BETTER_AUTH_SECRET=
BETTER_AUTH_URL=
DATABASE_URL=
AUTHENTIK_URL=
AUTHENTIK_CLIENT_ID=
AUTHENTIK_CLIENT_SECRET=
BUNNYCDN_PASSWORD=
# Encrypts infra secrets (SSH key, GPG passphrase) at rest. Keep distinct from BETTER_AUTH_SECRET.
FORGE_SECRETS_KEY=
# Max accepted .flatpak bundle upload size in bytes (default: 2 GiB if unset)
FLATPAK_MAX_BUNDLE_BYTES=
